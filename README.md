# snax-forge
SNAX's program to intermediate representation (IR) to accelerator design that is suited to fit within the SNAX framework. This work is inspired by the use of SDFG which gets transformed into a set of accelerators that fit the SNAX compute cluster, along with generated library kernels.
